# File: .github/workflows/Auto_sync_current_repo_Template.yml
# Need add secrets.PAT

name: sync-current-repo
on:
  workflow_dispatch:
 # schedule:
   # - cron: '0 1 * * 1'
    # UTC时区，比我们东八区早，上面示例为：每周一9点。

jobs:
  sync-current-repo:
    runs-on: ubuntu-latest
    if: github.event.repository.owner.id == github.event.sender.id

    steps:  
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: repo-sync
        uses: repo-sync/github-sync@v2
        with:
          source_repo: "https://github.com/zerotier/ZeroTierOne"
          source_branch: "*"
          destination_branch: "*"
          sync_tags: "true"
          github_token: ${{ secrets.PAT }}
          
